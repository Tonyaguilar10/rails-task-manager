<br>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>| <%= link_to "X", task_delete_path(task), method: :delete,
            data: { confirm: "Are you sure?" } %> </li>
  <% end %>
</ul>
<%= link_to "Add a task", task_new_path %>
